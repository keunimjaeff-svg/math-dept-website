<%- include('partials/head', {pageTitle: lang==='uz' ? 'Kafedra haqida' : 'О кафедре'}) %>
<body>
<%- include('partials/header') %>
<main>
  <div class="page-hero">
    <div class="container">
      <h1><%= lang==='uz' ? 'Kafedra haqida' : 'О кафедре' %></h1>
    </div>
  </div>
  <section class="section">
    <div class="container">
      <div class="about-text">
        <p><%= lang==='uz'
          ? "Oliy va amaliy matematika kafedrasi — Toshkent davlat iqtisodiyot universitetining yetakchi kafedralaridan biri bo'lib, talabalar uchun yuqori sifatli matematik ta'lim beradi."
          : 'Кафедра высшей и прикладной математики является одной из ведущих кафедр Ташкентского государственного экономического университета, обеспечивающей высококачественное математическое образование студентов.' %></p>
        <p><%= lang==='uz'
          ? "Kafedra professor-o'qituvchilari tomonidan bakalavrlik, magistrlik va doktorlik dissertatsiyalari, monografiyalar, ilmiy maqolalar, o'quv adabiyotlari nashr etib kelinmoqda."
          : 'Профессорско-преподавательским составом кафедры публикуются бакалаврские, магистерские и докторские диссертации, монографии, научные статьи и учебная литература.' %></p>
      </div>

      <% if (teachers.length > 0) { %>
      <h2 class="section-title"><%= lang==='uz' ? "Kafedra professor-o'qituvchilari" : 'Профессорско-преподавательский состав кафедры' %></h2>
      <div class="teachers-grid">
        <% teachers.forEach(t => { %>
        <div class="teacher-card">
          <div class="teacher-photo">
            <% if (t.photo) { %>
              <img src="<%= t.photo %>" alt="<%= t.name %>" loading="lazy">
            <% } else { %>
              <div class="teacher-avatar"><%= t.name.charAt(0) %></div>
            <% } %>
          </div>
          <div class="teacher-info">
            <h3><%= t.name %></h3>
            <% if (t.degree_ru || t.degree_uz) { %>
            <p class="teacher-degree"><%= lang==='uz' ? (t.degree_uz||t.degree_ru) : (t.degree_ru||t.degree_uz) %></p>
            <% } %>
            <% if (t.position_ru || t.position_uz) { %>
            <p class="teacher-pos"><%= lang==='uz' ? (t.position_uz||t.position_ru) : (t.position_ru||t.position_uz) %></p>
            <% } %>
            <% if (t.email) { %><a href="mailto:<%= t.email %>" class="teacher-email">✉️ <%= t.email %></a><% } %>
          </div>
        </div>
        <% }) %>
      </div>
      <% } else { %>
      <p class="empty-msg"><%= lang==='uz' ? "O'qituvchilar hali qo'shilmagan" : 'Преподаватели ещё не добавлены' %></p>
      <% } %>
    </div>
  </section>
</main>
<%- include('partials/footer') %>
</body></html>
