<%- include('layout', {pageTitle: 'Новости'}) %>
<div class="admin-page">
  <div class="admin-page-header">
    <h1 class="admin-title">Новости</h1>
    <a href="/admin/news/new" class="btn-add">+ Добавить</a>
  </div>
  <% if (list.length === 0) { %>
    <p class="empty-admin">Новостей нет. <a href="/admin/news/new">Добавить первую</a></p>
  <% } else { %>
  <table class="admin-table">
    <thead><tr><th>Заголовок (RU)</th><th>Статус</th><th>Дата</th><th>Действия</th></tr></thead>
    <tbody>
      <% list.forEach(item => { %>
      <tr>
        <td><a href="/news/<%= item.id %>" target="_blank"><%= item.title_ru %></a></td>
        <td><span class="badge <%= item.published ? 'badge-green' : 'badge-gray' %>"><%= item.published ? 'Опубликовано' : 'Скрыто' %></span></td>
        <td><%= new Date(item.created_at).toLocaleDateString('ru-RU') %></td>
        <td class="actions">
          <a href="/admin/news/<%= item.id %>/edit" class="btn-edit">✏️</a>
          <form method="POST" action="/admin/news/<%= item.id %>/delete" style="display:inline" onsubmit="return confirm('Удалить?')">
            <button type="submit" class="btn-del">🗑️</button>
          </form>
        </td>
      </tr>
      <% }) %>
    </tbody>
  </table>
  <% } %>
</div>
<%- include('layout-end') %>
